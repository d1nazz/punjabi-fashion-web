[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  targetPort = 8080
  port = 8888

[functions]
  directory = "netlify/functions"
  # Ensure server-side catalog JSON is packaged with the function bundle.
  included_files = ["netlify/functions/_data/**"]
